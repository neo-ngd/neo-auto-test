import logging

# logging.basicConfig(level=logging.INFO,
#                     filename='check.log',
#                     filemode='w',
#                     format=
#                     '%(asctime)s - %(pathname)s[line:%(lineno)d] - %(levelname)s: %(message)s'
#                     )
logging.basicConfig(level=logging.INFO)